import Cancel from "@/components/Pages/Payment/Cancel";
import React from "react";

export const metadata = {
  title: "Payment Cancel | 1Plus1 Dating website",
  description: "Cancel page",
  keywords: ["cancel", "page", "example"],
}
const page = () => {
  return <Cancel />;
};

export default page;
